"""Scaffolded phases — interfaces the core already anticipates, not yet built.

Each stub is a `typing.Protocol` (structural interface) plus a concrete
placeholder that raises `NotImplementedError` with a roadmap pointer. Filling one
in is *implementing an interface that already has a defined shape and tests*, not
refactoring the core. See ROADMAP.md. None of these is complete; the README says
so plainly.
"""
